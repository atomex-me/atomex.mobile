<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ffimageloadingsvg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
             x:Class="atomex.MyWalletsPage"
             Title="My wallets"
             BackgroundColor="{DynamicResource PageBackgroundColor}">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="PLEASE SELECT YOUR WALLET"
                   TextColor="{DynamicResource MainTextColor}"
                   FontFamily="{DynamicResource MediumFont}"
                   Padding="20,40"
                   FontSize="22"
                   HorizontalTextAlignment="Center"/>
            <Frame Style="{DynamicResource MainNetWalletFrameStyle}">
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnWalletTapped"/>
                </Frame.GestureRecognizers>
                <StackLayout Orientation="Horizontal">
                    <ffimageloadingsvg:SvgCachedImage WidthRequest="48"
                                                      HeightRequest="48"
                                                      Margin="10,0"
                                                      Source="Wallet__white.svg"/>
                    <Label Text="ATOMEX"
                           Style="{DynamicResource MainNetWalletlButtonTextStyle}"/>
                    <ffimageloadingsvg:SvgCachedImage Source="Right-arrow-2__white.svg"
                                                      HeightRequest="12"/>
                </StackLayout>
            </Frame>
            <Frame Style="{DynamicResource TestNetWalletFrameStyle}">
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnWalletTapped"/>
                </Frame.GestureRecognizers>
                <StackLayout Orientation="Horizontal">
                    <ffimageloadingsvg:SvgCachedImage WidthRequest="48"
                                                      HeightRequest="48"
                                                      Margin="10,0"
                                                      Source="Wallet__blue.svg"/>
                    <Label Text="STARIK"
                           Style="{DynamicResource TestNetWalletlButtonTextStyle}"/>
                    <ffimageloadingsvg:SvgCachedImage Source="Right-arrow-2__blue.svg"
                                                      HeightRequest="12"/>
                </StackLayout>
            </Frame>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
