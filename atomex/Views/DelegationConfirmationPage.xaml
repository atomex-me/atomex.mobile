<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resources="clr-namespace:atomex.Resources"
             x:Class="atomex.DelegationConfirmationPage"
             Title="{x:Static resources:AppResources.ConfirmationLabel}"
             BackgroundColor="{DynamicResource AdditionalBackgroundColor}">
    <ContentPage.Content>
        <RelativeLayout>
            <ActivityIndicator x:Name="SendingLoader"
                               RelativeLayout.XConstraint= "{ConstraintExpression Type=RelativeToParent, Property=Width, Factor = .5}"
	             	           RelativeLayout.YConstraint="{ConstraintExpression Type=RelativeToParent, Property=Height, Factor =.5}"
                               Scale="3"
                               Color="Black"/>
            <StackLayout x:Name="Content"
                         VerticalOptions="FillAndExpand">
                <TableView Intent="Form"
                           RowHeight="70">
                    <TableRoot>
                        <TableSection Title="{x:Static resources:AppResources.WantToDelegate}">
                            <TextCell Text="{x:Static resources:AppResources.FromLabel}"
                                      Detail="{Binding WalletAddressViewModel.WalletAddress.Address}"
                                      DetailColor="{DynamicResource HintTextColor}"/>
                            <TextCell Text="{x:Static resources:AppResources.AmountLabel}"
                                      Detail="{Binding WalletAddressViewModel.WalletAddress.Balance}"
                                      DetailColor="{DynamicResource HintTextColor}"/>
                            <TextCell Text="{x:Static resources:AppResources.ToLabel}"
                                      Detail="{Binding Address}"
                                      DetailColor="{DynamicResource HintTextColor}"/>
                            <TextCell Text="{x:Static resources:AppResources.WithCommision}"
                                      Detail="{Binding FeeString}"
                                      DetailColor="{DynamicResource HintTextColor}"/>
                            <ViewCell>
                                <StackLayout Padding="20,10">
                                    <Button Text="{x:Static resources:AppResources.DelegateButton}"
                                            x:Name="DelegateButton"
                                            Style="{DynamicResource MainButtonStyle}"
                                            Clicked="OnDelegateButtonClicked"/>
                                </StackLayout>
                            </ViewCell>
                        </TableSection>
                    </TableRoot>
                </TableView>
            </StackLayout>
        </RelativeLayout>
    </ContentPage.Content>
</ContentPage>
