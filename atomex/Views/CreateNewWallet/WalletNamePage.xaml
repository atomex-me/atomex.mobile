<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:atomex.CustomElements"
             xmlns:resources="clr-namespace:atomex.Resources"
             x:Class="atomex.Views.CreateNewWallet.WalletNamePage"
             Title="{x:Static resources:AppResources.CreateNewWalletPageTitle}"
             BackgroundColor="{DynamicResource AdditionalBackgroundColor}">
    <ScrollView x:Name="ScrollView">
        <StackLayout HorizontalOptions="CenterAndExpand"
                     VerticalOptions="CenterAndExpand">
            <StackLayout x:Name="Labels">
                <Label Text="{x:Static resources:AppResources.WalletNameStepLabel}"
                       Style="{DynamicResource CreateNewWalletTitleStyle}"/>
                <Label Text="{x:Static resources:AppResources.EnterNameLabel}"
                       Style="{DynamicResource CreateNewWalletTextStyle}"/>
            </StackLayout>
            <Frame x:Name="Frame"
                   Style="{DynamicResource EntryFrameStyle}"
                   CornerRadius="10">
                <StackLayout HeightRequest="60">
                    <Label x:Name="NameHint"
                           Style="{DynamicResource EntryHintStyle}"/>
                    <controls:CustomEntry x:Name="Entry"
                                          Placeholder="{x:Static resources:AppResources.WalletNameEntryPlaceholder}"
                                          Keyboard="Text"
                                          Focused="EntryFocused"
                                          Unfocused="EntryFocused"
                                          Style="{DynamicResource EntryStyle}"
                                          TextChanged="OnTextChanged"
                                          Text="{Binding WalletName}"/>
                </StackLayout>
            </Frame>
            <Label x:Name="Error"
                   Style="{DynamicResource ErrorTextStyle}"
                   IsVisible="False"/>
            <Button Text="{x:Static resources:AppResources.NextButton}"
                    Margin="0,20"
                    Style="{DynamicResource MainButtonStyle}"
                    Clicked="OnNextButtonClicked"/>
        </StackLayout>
    </ScrollView>
</ContentPage>
