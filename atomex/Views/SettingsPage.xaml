<?xml version="1.0" encoding="UTF-8"?>
  <ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:ffimageloadingsvg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
               x:Class="atomex.SettingsPage"
               Title="Settings"
               Icon="NavBar__settings">
        <ContentPage.Content>
            <TableView Intent="Settings"
                       BackgroundColor="{DynamicResource MainBackgroundColor}">
                <TableRoot>
                    <TableSection Title="Security"
                                  TextColor="{DynamicResource HintTextColor}">
                        <SwitchCell Text="Show Warnings"
                                    OnColor="{DynamicResource MainButtonBackgroundColor}"
                                    On="{Binding ShowActiveSwapWarning, Mode=TwoWay}">
                        </SwitchCell>
                        <SwitchCell Text="Automatic sign out"
                                    OnColor="{DynamicResource MainButtonBackgroundColor}"
                                    On="{Binding AutoSignOut, Mode=TwoWay}"/>
                        <ViewCell Tapped="OnPeriodOfInactiveSettingTapped">
                            <ViewCell.View>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="6*"/>
                                        <ColumnDefinition Width="3*"/>
                                        <ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Text="Period of inactive"
                                           Style="{DynamicResource OptionLabelStyle}">
                                    </Label>
                                    <Label Grid.Column="1"
                                           Text="{Binding PeriodOfInactivityInMin}"
                                           Style="{DynamicResource OptionValueLabelStyle}">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="{Binding PeriodOfInactivityInMin}"/>
                                                <Span Text=" min"/>
                                        </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                    <ffimageloadingsvg:SvgCachedImage Source="Right-arrow-2__black.svg"
                                                                      Grid.Column="2"
                                                                      Margin="10,16"/>
                                </Grid>
                            </ViewCell.View>
                        </ViewCell>
                    </TableSection>
                    <TableSection Title="Blockchain"
                                  TextColor="{DynamicResource HintTextColor}">
                        <ViewCell Tapped="OnBalanceUpdateIntervalSettingTapped">
                            <ViewCell.View>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="6*"/>
                                        <ColumnDefinition Width="3*"/>
                                        <ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Text="Balance update interval"
                                           Style="{DynamicResource OptionLabelStyle}">
                                    </Label>
                                    <Label Grid.Column="1"
                                           Text="{Binding BalanceUpdateIntervalInSec}"
                                           Style="{DynamicResource OptionValueLabelStyle}">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="{Binding BalanceUpdateIntervalInSec}"/>
                                                <Span Text=" sec"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                    <ffimageloadingsvg:SvgCachedImage Source="Right-arrow-2__black.svg"
                                                                      Grid.Column="2"
                                                                      Margin="10,16"/>
                                </Grid>
                            </ViewCell.View>
                        </ViewCell>
                    </TableSection>
                </TableRoot>
            </TableView>
        </ContentPage.Content>
    </ContentPage>